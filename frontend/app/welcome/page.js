'use client'
import { LandingPage } from '../../components/landing/LandingPage'

// Personal entry: show the LandingPage hero ("Not a tool. A presence.", BEGIN) FIRST.
// BEGIN → /welcome/start (the sign-in / SplitChoice). Don't drop users onto sign-in directly.
export default function WelcomePage() {
  return <LandingPage />
}
