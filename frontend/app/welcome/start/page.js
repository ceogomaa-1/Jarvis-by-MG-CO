'use client'
import SplitChoice from '../../../components/onboarding/SplitChoice'

// The sign-in / mode-choice screen, reached from the LandingPage hero's BEGIN button.
export default function WelcomeStartPage() {
  return <SplitChoice />
}
