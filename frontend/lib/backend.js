// Single source of truth for the Jarvis backend origin (Render).
// Every frontend module imports BACKEND from here instead of hardcoding the URL,
// so a host change is a one-line edit rather than 30+.
export const BACKEND = 'https://jarvis-backend-4oz6.onrender.com'
