# Pinned to the exact versions that pass the backend test suite (407 passed).
# Pin, don't float: a redeploy must never silently pull a breaking major of
# anthropic / fastapi / httpx. Bump deliberately, then re-run the suite.
anthropic==0.96.0
mem0ai==2.0.2
fastapi==0.136.0
uvicorn[standard]==0.45.0
python-dotenv==1.2.2
httpx==0.28.1
qdrant-client==1.17.1
pypdf==6.13.1
python-docx==1.2.0
openai==2.36.0
python-multipart==0.0.26
APScheduler==3.11.2
reportlab==4.5.1
supabase==2.31.0
cartesia==3.2.0
deepgram-sdk==3.11.0
trafilatura==2.1.0
python-pptx==1.0.2
playwright==1.60.0
pywebpush==2.3.0
dateparser==1.4.0
youtube-transcript-api==1.2.4
# Sales Advisor stealth scraping: scrapling base = adaptive parser only (light deps);
# curl_cffi = the browser-impersonation engine its Fetcher uses. Deliberately NOT
# scrapling[fetchers] — that extra pins its own playwright and would fight our 1.60.0 pin.
scrapling==0.4.11
curl_cffi==0.15.0
